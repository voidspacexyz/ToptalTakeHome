---
# =============================================================================
# .env.example — node-3tier-app2/
# Copy to .env and fill in real values before running docker-compose.
# NEVER commit .env with real secrets.
# =============================================================================

# ---------------------------------------------------------------------------
# Docker / Registry
# ---------------------------------------------------------------------------
# Image tag to build / pull (e.g. latest, v1.0.0, or a Git SHA)
TAG=latest

# ACR login server (used as image prefix)
ACR_SERVER=nodeprodacr.azurecr.io

# ---------------------------------------------------------------------------
# API — PostgreSQL connection (passed to the api container)
# ---------------------------------------------------------------------------
DBUSER=apiuser
DBPASS=changeme
DB=appdb
DBHOST=postgres          # service name inside docker-compose network
DBPORT=5432

# ---------------------------------------------------------------------------
# Web — API upstream (passed to the web container)
# ---------------------------------------------------------------------------
# URL of the api service as seen from inside the docker-compose network
API_HOST=http://api:3000

# ---------------------------------------------------------------------------
# PostgreSQL (postgres service — only used for local docker-compose)
# ---------------------------------------------------------------------------
POSTGRES_USER=apiuser
POSTGRES_PASSWORD=changeme
POSTGRES_DB=appdb

# ---------------------------------------------------------------------------
# Redis (web tier cache — optional; omit REDIS_URL to disable caching)
# ---------------------------------------------------------------------------
REDIS_URL=redis://redis:6379
