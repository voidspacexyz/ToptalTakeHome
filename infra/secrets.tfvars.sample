# ---------------------------------------------------------------------------
# secrets.tfvars.sample — Reference for required secret variables
#
# Copy this file to secrets.tfvars and fill in real values.
# secrets.tfvars is gitignored and must NEVER be committed.
#
# Usage:
#   tofu plan  -var-file="secrets.tfvars"
#   tofu apply -var-file="secrets.tfvars"
# ---------------------------------------------------------------------------

subscription_id   = "<your-azure-subscription-id>"
pg_admin_password = "<strong-password-min-8-chars-mixed-case-number-symbol>"
